model User {
  id            String    @id @default(uuid())
  email         String    @unique
  password_hash String
  fullName      String
  clientTier    String    @default("Sovereign")
  kycStatus     String    @default("Pending")
  createdAt     DateTime  @default(now())
}